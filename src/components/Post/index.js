import React, { Component } from 'react'
import PropTypes from 'prop-types'

class Post extends Component {
  static defaultProps = {
  }

  static propTypes = {
  }

  state = {
  }



  render() {
    return (<div>Post</div>)
  }
}

export default Post
